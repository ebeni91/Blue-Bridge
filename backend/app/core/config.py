# In a real app, use a strong random secret key stored in .env
SECRET_KEY = "blue_bridge_super_secret_key_change_this_in_production"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 # 24 hours expiration